{
  "name": "lambdacg-resolver-test",
  "main": "dist/index",
  "types": "dist/index",
  "declaration": true,
  "declarationMap": true,
  "workspaces": [
    "../src",
    "../../lambdacg-contract/src"
  ],
  "scripts": {
    "test": "npm install && tsc --build && mocha -r 'ts-node/register' **/*.test.ts",
    "test-watch": "mocha --require ts-node/register --extensions ts,tsx --watch --watch-files '../src' --watch-files . '**/*.test.ts'"
  },
  "devDependencies": {
    "@types/chai": "^4.2.22",
    "@types/chai-as-promised": "^7.1.4",
    "@types/mocha": "^9.0.0",
    "@types/sinon": "^10.0.6",
    "chai": "^4.3.4",
    "chai-as-promised": "^7.1.1",
    "mocha": "^9.1.3",
    "sinon": "^12.0.1",
    "ts-node": "^10.4.0"
  }
}
