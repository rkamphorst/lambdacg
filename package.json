{
    "name": "lambdacg",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "test": "yarn workspaces foreach -v run test",
        "test-coverage": "yarn workspaces foreach -v run test-coverage",
        "component-test": "yarn workspaces foreach -v run component-test",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix"
    },
    "devDependencies": {
        "@typescript-eslint/eslint-plugin": "^5.21.0",
        "@typescript-eslint/parser": "^5.21.0",
        "eslint": "^8.14.0",
        "eslint-config-prettier": "^8.5.0",
        "eslint-plugin-prettier": "^4.0.0",
        "eslint-plugin-simple-import-sort": "^7.0.0",
        "prettier": "^2.6.2"
    }
}